---
// Index acts as a router/gateway based on session auth cookie
const user = (Astro.locals as any).user;

if (user) {
  const targetPath = user.role === 'admin' ? 'dashboard' : user.role;
  return Astro.redirect(`/${targetPath}`);
} else {
  return Astro.redirect('/login');
}
---
